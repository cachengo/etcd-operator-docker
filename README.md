# etcd-operator-docker
Cross-platform builds of Docker images for etcd-operator

## Releasing a new version
Make a pull request updating the VERSION file to the desired
version and let Jenkins do the rest.
